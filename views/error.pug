extends layout

block content
    section.desk
        h1=  `${error.message} - Status: ${error.status}`
        a.button-primary-small(href='/') Go to Home Page
        .paper
            pre.stack-trace= error.stack
        .tear